/*
    Cookie constants, used to define the cookie identifiers
    so that spelling is less of an issue.
*/

const CookieConstants = Object.freeze({
  name: "nsw",
  content: {
    selectedLanguage: "selectedLanguage",
  },
});

export default CookieConstants;
